diff --git a/finetuning/sft_12hz.py b/finetuning/sft_12hz.py
index 6ec80c8..b09ee71 100644
--- a/finetuning/sft_12hz.py
+++ b/finetuning/sft_12hz.py
@@ -21,6 +21,7 @@ import shutil
 import torch
 from accelerate import Accelerator
 from dataset import TTSDataset
+from huggingface_hub import snapshot_download
 from qwen_tts.inference.qwen3_tts_model import Qwen3TTSModel
 from safetensors.torch import save_file
 from torch.optim import AdamW
@@ -46,1 +47,4 @@ def train():
     MODEL_PATH = args.init_model_path
+    local_model_path = MODEL_PATH if os.path.isdir(MODEL_PATH) else snapshot_download(MODEL_PATH)
+
+    attn_impl = os.environ.get("QWEN3_TTS_ATTN_IMPL", "sdpa")
@@ -50,5 +54,5 @@ def train():
     qwen3tts = Qwen3TTSModel.from_pretrained(
         MODEL_PATH,
         torch_dtype=torch.bfloat16,
-        attn_implementation="flash_attention_2",
+        attn_implementation=attn_impl,
     )
@@ -126,3 +130,3 @@ def train():
         if accelerator.is_main_process:
             output_dir = os.path.join(args.output_model_path, f"checkpoint-epoch-{epoch}")
-            shutil.copytree(MODEL_PATH, output_dir, dirs_exist_ok=True)
+            shutil.copytree(local_model_path, output_dir, dirs_exist_ok=True)
@@ -130,2 +134,2 @@ def train():
-            input_config_file = os.path.join(MODEL_PATH, "config.json")
+            input_config_file = os.path.join(local_model_path, "config.json")
             output_config_file = os.path.join(output_dir, "config.json")
