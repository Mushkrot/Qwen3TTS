# Candidate Review Report

Use this template for Qwen3-TTS checkpoint candidate review after a training run has generated evaluation samples and automatic metrics.

Generated WAV files, checkpoints, metrics, and candidate manifests referenced by
this report are working artifacts. Do not commit generated WAVs, generated
checkpoints, generated `metrics.jsonl`, generated `candidate_manifest.json`,
raw source audio, or processed dataset chunks.

## Run Metadata

- Voice name:
- Run id:
- Generated at:
- Dataset build path:
- Manifest path:
- Prepared manifest path:
- Candidate manifest path:
- Base model:
- Speaker name:
- Training command summary:
- Epoch range evaluated:
- Stop reason:
- Epochs completed:
- Best epoch / score:
- Candidate floor:
- Limited status / reasons:
- Metrics backend versions:
- Report generated by:

## Dataset Gate

- Dataset passed `docs/DATASET_CONTRACT.md`: yes / no
- Voice filtering policy used:
- Initial-window rejection used: yes / no
- Quality report path:
- Quarantine report path:
- Accepted chunk spot-check completed: yes / no
- Notes:

## Candidate Ranking

| Rank | Candidate | Checkpoint | Score | Hard-gate status | Warnings | Review sample path |
|---:|---|---|---:|---|---|---|
| 1 | candidate_A_epochN | `runs/.../checkpoint-epoch-N` |  | pass / limited / reject |  | `samples/.../candidate_A_epochN/` |
| 2 | candidate_B_epochN | `runs/.../checkpoint-epoch-N` |  | pass / limited / reject |  | `samples/.../candidate_B_epochN/` |
| 3 | candidate_C_epochN | `runs/.../checkpoint-epoch-N` |  | pass / limited / reject |  | `samples/.../candidate_C_epochN/` |
| 4 | candidate_D_epochN | `runs/.../checkpoint-epoch-N` |  | pass / limited / reject |  | `samples/.../candidate_D_epochN/` |

## Early Stop Summary

Copy these values from `candidate_manifest.json.stop_summary` and the top-level
manifest fields.

- `min_epochs`: 2
- `max_epochs`: 6
- `patience`: 2
- `top_candidates`: 4
- `candidate_floor`:
- `run_stop.reason`: `min_epochs_pending` / `patience_exhausted` / `quality_degradation` / `max_epochs_reached` / hard failure
- `run_stop.epochs_completed`:
- `run_stop.best_epoch`:
- `run_stop.best_score`:
- Manifest status: ok / limited
- Limited reasons:

The early-stop logic narrows the run so the owner does not need to listen after
every epoch. The final winner below is still human-selected from the top
candidates. Copied candidate WAV export and selected-checkpoint persistence are
not part of the current automated flow unless a later run explicitly adds them.

## Automatic Metrics Summary

| Candidate | ASR/text match | Duration ratio | Pace | Clipping | Silence/onset/offset | Speaker similarity | Loss trend | Decision |
|---|---:|---:|---:|---:|---|---:|---|---|
| candidate_A_epochN |  |  |  |  |  |  |  | keep / reject |
| candidate_B_epochN |  |  |  |  |  |  |  | keep / reject |
| candidate_C_epochN |  |  |  |  |  |  |  | keep / reject |
| candidate_D_epochN |  |  |  |  |  |  |  | keep / reject |

## Hard-Gate Notes

Rejected checkpoints remain auditable in `metrics.jsonl` and
`candidate_manifest.json.rejected_checkpoints`; they must not appear in
`candidate_manifest.json.candidates`.

- Failed generations:
- ASR/text mismatch (`asr_text_mismatch`):
- Pace accelerated versus previous viable checkpoint (`pace_accelerated`):
- Clipping (`audio_clipping`):
- Too short / too long (`duration_too_short`, `duration_too_long`):
- Suspected cutoff (`suspected_cut`):
- Sharp score drop (`score_drop`):
- Level warning:
- Leading/trailing silence warning:
- Onset/offset artifact warning:
- Speaker similarity warning:
- Training log warning:
- Dataset gate warning:

## Listening Checklist

Score each item from 1 to 5 and keep short notes. Use the same generated phrase files for every candidate.

### Candidate A

- Sample path:
- Speaker similarity:
- Naturalness:
- Onset quality:
- Offset quality:
- Pace and rhythm:
- English pronunciation clarity:
- Notes:

### Candidate B

- Sample path:
- Speaker similarity:
- Naturalness:
- Onset quality:
- Offset quality:
- Pace and rhythm:
- English pronunciation clarity:
- Notes:

### Candidate C

- Sample path:
- Speaker similarity:
- Naturalness:
- Onset quality:
- Offset quality:
- Pace and rhythm:
- English pronunciation clarity:
- Notes:

### Candidate D

- Sample path:
- Speaker similarity:
- Naturalness:
- Onset quality:
- Offset quality:
- Pace and rhythm:
- English pronunciation clarity:
- Notes:

## Final User Choice

- Selected candidate:
- Selected checkpoint:
- Selected sample path:
- Reason for choice:
- Trade-offs accepted:
- Follow-up action:
- Owner/date:

## Artifact Policy Reminder

Do not commit:

- raw source audio under `datasets/voices/**/Input/`;
- processed chunks under `datasets/voices/**/Ready/`;
- generated checkpoints under `experiments/**/runs/`;
- generated WAV samples under `experiments/**/samples/`.
- generated `metrics.jsonl` and `candidate_manifest.json` files under run
  directories.

Commit only this filled report if it contains no private source paths, no secrets, and no large generated data.
