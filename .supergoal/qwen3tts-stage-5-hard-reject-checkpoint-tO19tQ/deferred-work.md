# Deferred Work

Stage 5 does not implement:

- Full automatic early stopping.
- Real copied candidate audio export package.
- Human selection persistence via `selected_checkpoint.json`.
- Real speaker embedding backend.

Those remain later stages. Stage 5 only ensures bad checkpoints cannot enter the final candidate list/manifest.

