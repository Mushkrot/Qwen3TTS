SUPERGOAL_PHASE_START
Phase: 3 of 4 — Add Report Template
Task: Add the human-facing candidate review report template for future top-3/top-4 checkpoint exports.
Type: brownfield, documentation, ML workflow
Mandatory commands: git diff --check
Acceptance criteria: 5
Evidence required: template path, template headings, command exit code
Depends on phases: 1, 2

## Why

Future training runs need a concrete report format so a user can choose the winning voice without inspecting every epoch manually.

## Work

- Create `docs/templates/CANDIDATE_REVIEW_REPORT.md`.
- Include run metadata, candidate ranking, automatic metrics summary, warnings, listening checklist, final choice, and artifact policy reminders.
- Ensure the template can be filled manually now and generated by a future orchestrator later.

## Acceptance criteria (all must pass — verify each in transcript)

- `docs/templates/CANDIDATE_REVIEW_REPORT.md` exists.
- The template includes run metadata: voice name, dataset build path, manifest path, model base, training command summary, epoch range, and generated-at timestamp.
- The template includes one table for candidate ranking with checkpoint, score, hard-gate status, warnings, and review sample path.
- The template includes a per-candidate listening checklist for similarity, naturalness, onset, offset, pace, pronunciation, and notes.
- The template includes a final user choice section and artifact policy reminders that generated WAV/checkpoint files are not committed.

## Mandatory commands (run each, surface last ~10 lines + exit code)

- `git diff --check`

## Evidence required in transcript

- Print the new template path.
- Print the top-level headings from the template.
- Print `git diff --check` exit code.

## Notes

- Keep the template Markdown-only and small enough to commit safely.

---

The agent will, during execution, print SUPERGOAL_PHASE_START (above),
do the work, then print SUPERGOAL_PHASE_VERIFY, MEMORY_SAVED, and
SUPERGOAL_PHASE_DONE in order. On failure, the agent follows the
3-strike recovery protocol in .supergoal/qwen3tts-stage-1-checkpoint-selection-pr-KHbXIj/PROTOCOL.md without further
instruction needed here.
